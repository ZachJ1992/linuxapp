sample_write.c 和 sample_read.c 是两个测试进程，
两个相互独立的进程，一个负责创建共享内存并写数据，紧接着删除共享内存；
另一个负责读同一块取共享内存，读到则退出。

测试结果表明：
1。共享内存使用munmap和shm_unlink删除后，共享内存及数据消失，另一读进程则无法读到数据。
2.如果开辟的共享内存不使用munmap和shm_unlink删除，并且进程退出，那么共享内存将不会被释放，共享内存文件存在于/dev/shm/文件夹下。
